{
  "name": "to-do-list",
  "version": "1.0.0",
  "description": "to-do-list with react and tailwindcss and mongodb/mongoose server full stack",
  "main": "./backend/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon ./backend/server.js"
  },
  "author": "lawrence yoon",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
